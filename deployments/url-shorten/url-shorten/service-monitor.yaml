apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: url-shorten-app
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      run: url-shorten
  endpoints:
  - port: http